<% content_for :title, "Editar servicio" %>

<div class="page-wrapper">
  <div class="page-header">
    <div>
      <h1 class="page-title">Editar servicio</h1>
      <p class="page-subtitle"><%= @service.name %></p>
    </div>
  </div>

  <%= render "form", service: @service %>

  <div class="action-bar">
    <%= link_to "← Volver", @service, class: "btn-sm btn-sm-secondary" %>
  </div>
</div>
